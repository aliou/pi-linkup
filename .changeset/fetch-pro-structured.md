---
"@aliou/pi-linkup": minor
---

Add Fetch Pro mode and structured extraction to `linkup_web_fetch`. The tool gains `mode` (`"standard"`/`"pro"`) and `schema`/`instructions` (typed JSON returned in `data` alongside the markdown) parameters, and documents PDF support up to 100 MB. Updates the balance command with Pro fetch rates and the bundled skill docs.
